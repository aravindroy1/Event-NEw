---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ticket-service-config
  namespace: event-management
data:
  PORT: "3004"
  MONGO_URI: "mongodb://event-mongo:27017/ticket-service"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ticket-service
  namespace: event-management
  labels:
    app: ticket-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ticket-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: ticket-service
    spec:
      containers:
      - name: ticket-service
        image: ticket-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3004
        envFrom:
        - configMapRef:
            name: ticket-service-config
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "100m"
            memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: ticket-service
  namespace: event-management
  labels:
    app: ticket-service
spec:
  type: ClusterIP
  selector:
    app: ticket-service
  ports:
  - port: 80
    targetPort: 3004
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ticket-service-hpa
  namespace: event-management
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ticket-service
  minReplicas: 1
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 80
